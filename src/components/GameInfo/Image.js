import React from 'react'

const Image = ({imgurl, title}) =>{
  
        return(

                <div>
                    <h1>{title}</h1>
                </div>

         );
}
export default Image